@model IEnumerable<NntDay09LabCF.Models.Nnt_LoaiSanPham>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nntMaLoai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nntTenLoai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nntMaLoai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nntTenLoai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.nntId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.nntId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.nntId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
